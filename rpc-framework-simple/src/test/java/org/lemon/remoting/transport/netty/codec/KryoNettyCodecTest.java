package org.lemon.remoting.transport.netty.codec;

import org.junit.jupiter.api.Test;
import org.lemon.remoting.dto.RpcRequest;
import org.lemon.remoting.dto.RpcResponse;
import org.lemon.serialize.kryo.KryoSerializer;

class KryoNettyCodecTest {

    @Test
    private void testCodec() {
//        KryoNettyDecoder decoder = new KryoNettyDecoder(new KryoSerializer(), RpcRequest.class);
//        KryoNettyEncoder encoder = new KryoNettyEncoder(new KryoSerializer(), RpcResponse.class);
    }

}